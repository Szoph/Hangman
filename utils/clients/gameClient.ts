class GameClient {
    async uploadGameData(gameState: any): Promise<void> {
        // setTimeout(() => {
            console.log(gameState);
        // }, 3000)
    }
 
    worldRanking(): void {
        
    }
}

export default new GameClient;